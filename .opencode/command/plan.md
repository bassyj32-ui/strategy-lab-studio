---
description: Plan a Strategy Lab Studio feature with the ARCHITECT (read-only design + docs). Use for design before any code.
agent: architect
---

Plan the following Strategy Lab Studio (MVP-1) work using the ARCHITECT agent.

Request: $ARGUMENTS

Scope check: confirm this is P0 / MVP-1. If it implies P1–P3 features, say so
and do not design them. Have the ARCHITECT read `AGENTS.md`,
`docs/architecture.md`, `docs/decisions.md`, `docs/rendering.md`, and the
current source, then return a concrete plan: file layout, types, function
signatures, and implementation order for the BUILDER.
