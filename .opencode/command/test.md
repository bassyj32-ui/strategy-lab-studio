---
description: Write or run tests for Strategy Lab Studio with the BUILDER using the testing skill.
agent: builder
---

Test the following Strategy Lab Studio (MVP-1) area using the BUILDER agent
and the testing skill workflow.

Area / request: $ARGUMENTS

Add or run tests that cover the behavior. Verify with `npm run check` (the unit
step must pass — green). Stay inside MVP-1 scope and keep tests minimal but
meaningful. The BUILDER cannot push to git.
